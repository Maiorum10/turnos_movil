import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.turnos.app',
  appName: 'Turnos GADM Mocha',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
